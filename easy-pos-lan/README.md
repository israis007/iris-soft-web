# Easy POS LAN — landing publicada por IRIsoft

Esta landing se publica bajo `/easy-pos-lan/` en `irisoft.com.mx`.

Fuente de producto: `../../antigravity/web` en el workspace local (`easy-pos-lan.git`). Si se modifica la landing original, replica los cambios aquí y valida los enlaces antes de publicar.
